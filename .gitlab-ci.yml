build-job:
  stage: build
  script:
    - ./build.sh

test-job:
  stage: test
  needs: ["build-job"]
  dependencies:
    - build-job
  script:
    - cd ./gimbalsaurus
    - xcodebuild test -scheme gimbalsaurusTests
